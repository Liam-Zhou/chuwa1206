type Post{
    id: ID
    title: String
    description: String
    content: String
}

type Query{
    getPostById(id: ID): Post
    getAllPosts: [Post]
}

type Mutation{
    createPost(title:String!, description: String!, content: String!) : Post! # 感叹号表示强制
}

# Query 和 Mutation 只能是唯一的，其他可以定义很多