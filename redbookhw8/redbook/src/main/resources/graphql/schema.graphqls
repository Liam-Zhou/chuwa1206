type Post {
    id: ID
    title: String
    content: String
    description: String
}

type Author{
    id: ID
    name: String
}

type Query{
    getPostById(id: ID): Post
    getAllPosts: [Post]
}

type Mutation{
    createPost(title:String!, description:String!, content:String!): Post!
}