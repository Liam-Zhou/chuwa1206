type Post{
    id: ID
    title: String
    description: String
    content: String
}

type Query{
    getAllPosts: [Post]
}

type Mutation {
    createPost(title: String!, description: String!, content: String!): Post!
}